machine:
  services:
    - docker

dependencies:
  override:
    - docker pull ambermd/amber-win-cross

test:
  override:
      - docker run -it --rm ambermd/ambermd-win-cross bash scripts/build_netcdf.sh
  post:
    - echo "nothing"
    # - mkdir $CIRCLE_ARTIFACTS/pytraj-build
    # - cp dist/wheelhouse/pytraj*whl $CIRCLE_ARTIFACTS/pytraj-build/
