machine:
  services:
    - docker

dependencies:
  override:
    - docker pull ambermd/amber-win-cross

test:
  override:
      - docker run -it --rm -v `pwd`:/work -w /work ambermd/ambermd-win-cross bash scripts/build_netcdf.sh
  post:
    - ls local/
    # - mkdir $CIRCLE_ARTIFACTS/pytraj-build
    # - cp dist/wheelhouse/pytraj*whl $CIRCLE_ARTIFACTS/pytraj-build/
